// Guidelines for effective use of comments

// Good example
let discountPercentage = 0.1; // 10% discount for loyal customers

function fetchData(url) {
    // fetch data from API endpoint
}

// Bad example
let totalPrice = calculateTotal(items); // Calculate total price

function processOrder(order) {
    // unclear logic without comments
}
